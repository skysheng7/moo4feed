% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feed_water_summary.R
\name{feed_water_summary}
\alias{feed_water_summary}
\title{Summarize and check for abnormal feed & water intake}
\usage{
feed_water_summary(
  feed = NULL,
  water = NULL,
  warn = NULL,
  cfg = qc_config(),
  id_col = id_col2(),
  intake_col = intake_col2(),
  dur_col = duration_col2()
)
}
\arguments{
\item{feed}{Either a data frame containing all feed visits across days,
or a list of data frames grouped by date.
If providing a list, each data frame must have a 'date' column.}

\item{water}{Either a data frame containing all water visits across days,
or a list of data frames grouped by date.
If providing a list, each data frame must have a 'date' column.}

\item{warn}{Warning data frame to update}

\item{cfg}{A configuration list created by \code{\link[=qc_config]{qc_config()}}.}

\item{id_col}{Animal ID column name (default current global value from \code{\link[=id_col2]{id_col2()}})}

\item{intake_col}{Intake column name (default current global value from \code{\link[=intake_col2]{intake_col2()}})}

\item{dur_col}{Duration column name (default current global value from \code{\link[=duration_col2]{duration_col2()}})}
}
\value{
A list of:
\itemize{
\item \code{summary}: merged data frame of daily intake, duration, and visit counts
\item \code{warn}: updated warnings
}
}
\description{
This function summarizes daily feed and water intake, visit duration, and counts,
and updates intake warnings (if input warning dataframe \code{warn} is not \code{NULL}).
If you don't want to update warnings, set \code{warn = NULL}, and only the summary will be returned.
}
\examples{
# Example with single data frames
feed_df <- tibble::tibble(
  date = as.Date(c("2024-01-01", "2024-01-01")),
  cow = c("A", "B"),
  intake = c(20, 80),
  duration = c(200, 300)
)

water_df <- tibble::tibble(
  date = as.Date(c("2024-01-01", "2024-01-01")),
  cow = c("A", "B"),
  intake = c(50, 200),
  duration = c(100, 150)
)

# Example with lists of data frames
feed_list <- list(
  "2024-01-01" = tibble::tibble(
    date = as.Date("2024-01-01"),
    cow = c("A", "B"),
    intake = c(20, 80),
    duration = c(200, 300)
  ),
  "2024-01-02" = tibble::tibble(
    date = as.Date("2024-01-02"),
    cow = c("A", "C"),
    intake = c(25, 85),
    duration = c(210, 310)
  )
)

water_list <- list(
  "2024-01-01" = tibble::tibble(
    date = as.Date("2024-01-01"),
    cow = c("A", "B"),
    intake = c(50, 200),
    duration = c(100, 150)
  ),
  "2024-01-02" = tibble::tibble(
    date = as.Date("2024-01-02"),
    cow = c("A", "C"),
    intake = c(55, 210),
    duration = c(110, 160)
  )
)

warn <- tibble::tibble(
  date = as.Date(c("2024-01-01", "2024-01-02")),
  low_daily_feed_intake_cows = NA_character_,
  high_daily_feed_intake_cows = NA_character_,
  low_daily_water_intake_cows = NA_character_,
  high_daily_water_intake_cows = NA_character_
)

cfg <- qc_config()

# Using single data frames
feed_water_summary(feed_df, 
                  water_df, 
                  warn, 
                  cfg, 
                  id_col = "cow", 
                  intake_col = "intake", 
                  dur_col = "duration")
                  
# Using lists of data frames
feed_water_summary(feed_list, 
                  water_list, 
                  warn, 
                  cfg, 
                  id_col = "cow", 
                  intake_col = "intake", 
                  dur_col = "duration")

}
