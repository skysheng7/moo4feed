% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{moo4feed_example}
\alias{moo4feed_example}
\title{Access Example Data Files Shipped with \strong{moo4feed}}
\usage{
moo4feed_example(path = NULL)
}
\arguments{
\item{path}{\emph{Optional.} A single character string giving the name of an
example file to retrieve. Use \code{NULL} (the default) to list all available
example files.}
}
\value{
\itemize{
\item \strong{\code{path = NULL}} — a character vector of file names contained in the
package’s \code{extdata} directory.
\item \strong{\code{path} is a file name} — a single character string with the full path to
that file.
}
}
\description{
A lightweight helper that exposes the data files located in the package’s
\verb{inst/extdata/} directory.
\itemize{
\item If \code{path} is \code{NULL} (default) the function \strong{lists} every file available
in that directory, making it easy to discover the bundled examples.
\item If \code{path} is a single file name (e.g., \code{"VR201102.DAT"}), the function
returns the \strong{absolute path} to that file so it can be opened with
functions such as \code{\link[=read_data_safely]{read_data_safely()}} or base‐R I/O utilities.
}
}
\details{
Internally this is a thin wrapper around \code{\link[base:system.file]{base::system.file()}}, so it inherits
its behaviour—most notably the \code{mustWork = TRUE} argument, which triggers an
error if the requested file does not exist.
}
\examples{
# List every example data file shipped with the package
moo4feed_example()

# Retrieve the full path to one specific file
file_path <- moo4feed_example("VR201101.DAT")
file_path

}
