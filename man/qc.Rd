% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quality_check.R
\name{qc}
\alias{qc}
\title{Run full quality-control check on feeder and drinker data}
\usage{
qc(
  feed = NULL,
  water = NULL,
  cfg = qc_config(),
  id_col = id_col2(),
  start_col = start_col2(),
  end_col = end_col2(),
  bin_col = bin_col2(),
  dur_col = duration_col2(),
  intake_col = intake_col2(),
  verbose = TRUE
)
}
\arguments{
\item{feed}{A list of daily \strong{feed} data frames named by date, or \code{NULL}
if you don't have feeder data}

\item{water}{A list of daily \strong{water} data frames named by date, or \code{NULL}
if you don't have water data}

\item{cfg}{A configuration list created by \code{\link[=qc_config]{qc_config()}}.}

\item{id_col}{Animal ID column name (default current global value)}

\item{start_col}{Start time column name (default current global value)}

\item{end_col}{End time column name (default current global value)}

\item{bin_col}{Bin ID column name (default current global value)}

\item{dur_col}{Duration column name (default current global value)}

\item{intake_col}{Intake column name (default current global value)}

\item{verbose}{Logical. If TRUE, print details of data where errors were detected}
}
\value{
A list with four elements:
\describe{
\item{\code{warnings}}{a tidy data frame with one row per day and one column
per warning code.}
\item{\code{feed}}{cleaned feed list (or \code{NULL}).}
\item{\code{water}}{cleaned water list (or \code{NULL}).}
\item{\code{combined}}{merged feed + water list (after all fixes).}
}
}
\description{
Run full quality-control check on feeder and drinker data
}
\examples{
cfg <- qc_config(high_dur_feed = 2500, low_visit_threshold = 5)
out <- qc(feed = all_fed, water = all_wat, cfg = cfg)
out$warnings
out$feed
}
