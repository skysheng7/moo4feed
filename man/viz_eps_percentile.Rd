% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_eps_percentile.R
\name{viz_eps_percentile}
\alias{viz_eps_percentile}
\title{Visualize gap distribution and percentile-based optimal interval (eps) selection}
\usage{
viz_eps_percentile(
  data,
  percentile = 0.9,
  lower_bound = NULL,
  upper_bound = NULL,
  bins = 100,
  colors = hcl.colors(2, "Set 3"),
  title_prefix =
    "Distribution of time gap between visits & \\noptimal meal interval (eps)",
  xlim = 20,
  id_col = id_col2(),
  start_col = start_col2(),
  end_col = end_col2(),
  tz = tz2()
)
}
\arguments{
\item{data}{A single dataframe or list of dataframes containing feeding visit data}

\item{percentile}{Numeric value between 0 and 1 specifying which percentile to use
for eps determination. Default is 0.9.}

\item{lower_bound}{Numeric value for lower bound of the optimal interval, if NULL, no lower bound is applied.}

\item{upper_bound}{Numeric value for upper bound of the optimal interval, if NULL, no upper bound is applied.}

\item{bins}{Number of bins for the histogram. Default is 100.}

\item{colors}{Character vector of 2 colors to use. Default uses \code{hcl.colors(2, "Set 3")}.}

\item{title_prefix}{Character string for plot title prefix. Default is "Distribution of time gap between visits & optimal meal interval (eps)".}

\item{xlim}{Numeric value for x-axis limit. Default is 20.}

\item{id_col}{Animal ID column name (default current global value from \code{\link[=id_col2]{id_col2()}})}

\item{start_col}{Start time column name (default current global value from \code{\link[=start_col2]{start_col2()}})}

\item{end_col}{End time column name (default current global value from \code{\link[=end_col2]{end_col2()}})}

\item{tz}{Timezone (default current global value from \code{\link[=tz2]{tz2()}})}
}
\value{
A ggplot2 object showing the gap distribution histogram with optimal eps line
}
\description{
Creates a histogram visualization of inter-visit gaps with the optimal eps value
(determined using percentile method) highlighted as a vertical line. This helps
you understand how the percentile method selects the optimal interval for meal clustering.
\itemize{
\item If you wish to calculate optimal interval for a single animal across multiple days,
make sure data belongs to a single animal across multiple days.
\item If you wish to calculate optimal interval for all animals across all days,
make sure data recorded all animals across all days.
\item If you wish to calculate optimal interval for a single animal on a single day,
make sure data belongs to a single animal and only 1 day.
}
}
\details{
The function internally calls \code{\link[=meal_interval]{meal_interval()}} with method="percentile" to determine
the optimal eps value, then creates a histogram of all inter-visit gaps with the
optimal eps marked as a vertical dashed line.
}
\examples{
# Create toy dataset
toy_data <- all_fed[[1]][which(all_fed[[1]]$cow == 5114),]
# Visualize with 90th percentile
plot <- viz_eps_percentile(toy_data, id_col = "cow", 
                           start_col = "start", end_col = "end", 
                           tz = "America/Vancouver", 
                           percentile = 0.9)

}
