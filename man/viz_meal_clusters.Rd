% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_meal_clusters.R
\name{viz_meal_clusters}
\alias{viz_meal_clusters}
\title{Visualize meal clustering results as timeline plots}
\usage{
viz_meal_clusters(
  data,
  point_size = 2,
  point_alpha = 0.7,
  ncol_facet = 3,
  date_format = "\%Y-\%m-\%d",
  time_breaks = "4 hours",
  time_labels = "\%H",
  outlier_color = "grey50",
  title_size = 10,
  id_col = id_col2(),
  start_col = start_col2()
)
}
\arguments{
\item{data}{Merged visit data with meal assignments from \code{\link[=merge_cluster_results]{merge_cluster_results()}}.
Can be a single dataframe or list of dataframes.}

\item{point_size}{Numeric. Size of the points representing visits (default: 2).}

\item{point_alpha}{Numeric. Transparency of points, between 0 and 1 (default: 0.7).}

\item{ncol_facet}{Numeric. Number of columns for faceting when creating overview plots (default: 3).}

\item{date_format}{Character. Format for date labels (default: "\%Y-\%m-\%d").}

\item{time_breaks}{Character. Time axis breaks, passed to \code{\link[ggplot2:scale_date]{ggplot2::scale_x_datetime()}} (default: "4 hours").}

\item{time_labels}{Character. Time axis label format (default: "\%H").}

\item{outlier_color}{Character. Color for outlier points (meal_id = 0) (default: "grey50").}

\item{title_size}{Numeric. Size of plot titles when creating overview plots (default: 10).}

\item{id_col}{Animal ID column name (default current global value from \code{\link[=id_col2]{id_col2()}})}

\item{start_col}{Start time column name (default current global value from \code{\link[=start_col2]{start_col2()}})}
}
\value{
If ≤10 animal-day combinations: Single ggplot object with faceted plots.
If >10 combinations: Nested list structure:
\describe{
\item{plots}{List of plots organized by animal then date}
}
}
\description{
Creates timeline plots showing meal assignments over time. Points represent
individual feeding visits, colored by meal_id. Outliers (meal_id = 0) are
shown in red. The function can handle both single dataframes and lists of
dataframes from \code{\link[=merge_cluster_results]{merge_cluster_results()}}.
}
\details{
The function creates timeline plots with:
\itemize{
\item X-axis: Time of day (hours)
\item Y-axis: Date (vertical for single plots, horizontal for multiple stacked plots)
\item Points: Individual feeding visits
\item Colors: meal_id (red for outliers when meal_id = 0)
\item Title: "Animal XX" where XX is the animal ID
}
}
\examples{
# Create toy visit data with meal assignments
toy_data <- all_fed[[1]][which(all_fed[[1]]$cow == 5114),]

# Cluster and label meals
labeled <- meal_label_visits(toy_data, id_col = 'cow', start_col = 'start', 
end_col = 'end', bin_col = 'bin', intake_col = 'intake', dur_col = 'duration',
tz = 'America/Vancouver')

# Visualize meal clusters
p <- viz_meal_clusters(labeled, id_col = 'cow', start_col = 'start')

}
