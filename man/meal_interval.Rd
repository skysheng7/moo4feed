% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meal_optimal_interval.R
\name{meal_interval}
\alias{meal_interval}
\title{Calculate optimal interval between feeding visits for meal clustering}
\usage{
meal_interval(
  data,
  method = "gmm",
  percentile = 0.93,
  lower_bound = 5,
  upper_bound = 60,
  use_log_transform = TRUE,
  log_multiplier = 20,
  log_offset = 1,
  id_col = id_col2(),
  start_col = start_col2(),
  end_col = end_col2(),
  tz = tz2()
)
}
\arguments{
\item{data}{A single dataframe or list of dataframes containing feeding visit data}

\item{method}{Character string specifying the method for eps determination.
Options are "gmm" (default), "percentile", or "both".
\itemize{
\item "percentile": Uses the specified percentile of inter-visit gaps to determine
the optimal interval between feeding visits for meal clustering
\item "gmm": Uses Gaussian mixture modeling to identify
the optimal interval between feeding visits for meal clustering
\item "both": Uses both methods and returns the minimum (more conservative)
}}

\item{percentile}{Numeric value between 0 and 1 specifying which percentile to use
for eps determination when method="percentile" or "both". Default is 0.93.}

\item{lower_bound}{Numeric value for lower bound of the optimal interval, if NULL, no lower bound is applied.}

\item{upper_bound}{Numeric value for upper bound of the optimal interval, if NULL, no upper bound is applied.}

\item{use_log_transform}{Logical indicating whether to use log transformation for GMM fitting. Default is TRUE.
Log transformation often provides better separation of within-meal and between-meal gaps.}

\item{log_multiplier}{Numeric value for multiplier of log transformation. Default is 20.}

\item{log_offset}{Numeric value for offset of log transformation. Default is 1.}

\item{id_col}{Animal ID column name (default current global value from \code{\link[=id_col2]{id_col2()}})}

\item{start_col}{Start time column name (default current global value from \code{\link[=start_col2]{start_col2()}})}

\item{end_col}{End time column name (default current global value from \code{\link[=end_col2]{end_col2()}})}

\item{tz}{Timezone (default current global value from \code{\link[=tz2]{tz2()}})}
}
\value{
A numeric value representing the optimal eps parameter in minutes.
This value is bounded between lower_bound and upper_bound for practical meal clustering.
}
\description{
This function calculates the optimal interval between feeding visits for meal clustering
by analyzing the gaps between feeding visits for each animal within each day.
The function determines an appropriate time threshold that can be used to cluster
visits into meals based on temporal proximity.
\itemize{
\item If you wish to calculate optimal interval for a single animal across multiple days,
make sure data belongs to a single animal across multiple days.
\item If you wish to calculate optimal interval for all animals across all days,
make sure data recorded all animals across all days.
\item If you wish to calculate optimal interval for a single animal on a single day,
make sure data belongs to a single animal and only 1 day.
}
}
\details{
The function processes feeding visit data to calculate time gaps between consecutive
visits for each animal within each day. These gaps are then analyzed using statistical
methods to determine an optimal time threshold for clustering visits into meals.

The analysis considers:
\itemize{
\item Inter-visit gaps within each animal-day combination
\item Statistical distribution of gap durations
\item Conservative bounds (lower_bound-upper_bound) for practical application
}

When method="both", the function uses both percentile and Gaussian mixture modeling
approaches and returns the minimum value to be more conservative in meal definitions.
}
\examples{
# Calculate optimal eps using default method (GMM)
meal_interval(all_fed[[1]])

# Use only percentile method with 80th percentile
meal_interval(all_fed[[1]], method = "percentile", percentile = 0.8)

# Use only Gaussian mixture modeling
meal_interval(all_fed[[1]], method = "gmm")

# Work with list of dataframes
meal_interval(all_fed, method = "both", percentile = 0.93)

}
\seealso{
\code{\link[=cluster_meals]{cluster_meals()}} for using the eps parameter in meal clustering
}
