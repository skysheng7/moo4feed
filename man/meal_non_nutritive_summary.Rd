% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meal_visit_analysis.R
\name{meal_non_nutritive_summary}
\alias{meal_non_nutritive_summary}
\title{Analyze Non-Nutritive and Empty Bin Visits Within Meals}
\usage{
meal_non_nutritive_summary(
  data,
  cfg = qc_config(),
  id_col = id_col2(),
  intake_col = intake_col2(),
  start_weight_col = start_weight_col2()
)
}
\arguments{
\item{data}{A named list of daily data frames or a single data frame containing
visit records with meal labels. Must be output from \code{\link[=meal_label_visits]{meal_label_visits()}},
which adds the required \code{meal_id} and \code{meal_start} columns.}

\item{cfg}{A configuration list created by \code{\link[=qc_config]{qc_config()}}. Used to determine
calibration error threshold for classifying visits.}

\item{id_col}{Animal ID column name (default current global value from \code{\link[=id_col2]{id_col2()}})}

\item{intake_col}{Intake column name (default current global value from \code{\link[=intake_col2]{intake_col2()}})}

\item{start_weight_col}{Start weight column name (default current global value from \code{\link[=start_weight_col2]{start_weight_col2()}})}
}
\value{
A data frame (or named list of data frames) with columns:
\itemize{
\item \code{date} - Date
\item \verb{[id_col]} - Animal identifier
\item \code{mean_non_nutritive_per_meal} - Average non-nutritive visits per meal
\item \code{median_non_nutritive_per_meal} - Median non-nutritive visits per meal
\item \code{sd_non_nutritive_per_meal} - Standard deviation of non-nutritive visits
\item \code{mean_empty_bin_per_meal} - Average empty bin visits per meal
\item \code{median_empty_bin_per_meal} - Median empty bin visits per meal
\item \code{sd_empty_bin_per_meal} - Standard deviation of empty bin visits
\item \code{total_non_nutritive_visits} - Total non-nutritive visits
\item \code{total_empty_bin_visits} - Total empty bin visits
\item \code{total_meals} - Total number of meals
}
}
\description{
Analyzes visits within meals to identify and summarize non-nutritive visits
(no feed intake but feed available) and empty bin visits (no feed available).
Input data must already have meal labels from \code{\link[=meal_label_visits]{meal_label_visits()}}.
}
\examples{
# Create sample visit data with meal labels
visits <- data.frame(
  date = "2024-01-01",
  cow = c("A", "A", "A", "B", "B"),
  bin = c(1, 2, 3, 1, 2),
  start_weight = c(50, 0.01, 45, 48, 0.02),
  intake = c(2.5, 0.01, 0.02, 3.0, 0.01),
  meal_id = c(1, 1, 1, 1, 1),
  meal_start = as.POSIXct("2024-01-01 08:00:00", tz = "UTC")
)

# Analyze non-nutritive and empty bin visits
meal_visits <- meal_non_nutritive_summary(
  data = visits,
  cfg = qc_config()
)

}
