% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quality_check_setup.R
\name{qc_config}
\alias{qc_config}
\title{Build a configuration list for data quality-control}
\usage{
qc_config(
  high_dur_feed = 2000,
  high_dur_water = 1800,
  large_intake_feeder = 8,
  large_intake_drinker = 30,
  large_intake_rate_feed = 0.008,
  large_intake_rate_water = 0.35,
  low_visit_threshold = 10,
  total_cows_expected = NA,
  low_feed_intake = 35,
  high_feed_intake = 75,
  low_wat_intake = 60,
  high_wat_intake = 180,
  replacement_threshold = 26,
  calibration_error = 0.5,
  bin_offset = bin_offset2(),
  bins_feed = bins_feed2(),
  bins_wat = bins_wat2(),
  ...
)
}
\arguments{
\item{high_dur_feed}{Numeric. Seconds that define a \emph{long} feeding visit.}

\item{high_dur_water}{Numeric. Seconds that define a \emph{long} drinking visit.}

\item{large_intake_feeder}{Numeric. Kilograms that flag a single, unusually
large \strong{feed} intake event.}

\item{large_intake_drinker}{Numeric. Litres that flag a single, unusually
large \strong{water} intake event.}

\item{large_intake_rate_feed}{Numeric. Kilograms/s considered a
rapid feed-intake rate.}

\item{large_intake_rate_water}{Numeric. Litres/s considered a
rapid water-intake rate.}

\item{low_visit_threshold}{Integer. Bins visited fewer than this number of
times per day are flagged as \emph{low traffic}.}

\item{total_cows_expected}{Integer. Expected herd size; if \code{NA} (default) the
pipeline counts unique IDs automatically.}

\item{low_feed_intake}{Numeric. kg/day.  Daily feed intake flagged as \emph{low}
for a animal (default \strong{35}).}

\item{high_feed_intake}{Numeric. kg/day. Daily feed intake flagged as \emph{high}
for an animal (default \strong{75}).}

\item{low_wat_intake}{Numeric. L/day. Daily water intake flagged as \emph{low}
for an animal (default \strong{60}).}

\item{high_wat_intake}{Numeric. L/day. Daily water intake flagged as \emph{high}
for an animal (default \strong{180}).}

\item{replacement_threshold}{Numeric. Seconds. Time gap to classify replacement
behaviour (default \strong{26 s}).}

\item{calibration_error}{Numeric. Allowed feeder calibration error (default \strong{0.5} kg/L).}

\item{bin_offset}{Integer. Offset added to water-bin IDs so they do not
overlap with feed-bin IDs. Default derives from \code{\link[=bin_offset2]{bin_offset2()}}.}

\item{bins_feed}{Integer vector. Valid \strong{feed} bin IDs. Default
\code{\link[=bins_feed2]{bins_feed2()}}.}

\item{bins_wat}{Integer vector. Valid \strong{water} bin IDs. Default
\code{\link[=bins_wat2]{bins_wat2()}}.}

\item{...}{Reserved for future or project-specific tweaks. Named elements
here are appended to the returned list.}
}
\value{
A named list consumed by \code{\link[=qc]{qc()}} and its internal \verb{qc_*()}
helpers.
}
\description{
This helper centralises \strong{every threshold} used by \code{\link[=qc]{qc()}}.
It lets you define what should be flagged as \emph{abnormal} in your
feed–water data: extra-long bouts, unrealistically large intakes, bins with
too few visits, etc.  Each argument has a sensible default, so you normally
override only the handful you care about—then pass the resulting list to
\code{qc(cfg = qc_config(...))}.
}
\examples{
# Use all defaults
cfg <- qc_config()

# Tighten the "long feeding visit" threshold
cfg2 <- qc_config(high_dur_feed = 1800)

}
