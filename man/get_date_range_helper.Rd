% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/file_name_process.R
\name{get_date_range_helper}
\alias{get_date_range_helper}
\title{Get date range string for dataset}
\usage{
get_date_range_helper(df)
}
\arguments{
\item{df}{A data frame with a \code{date} column, it can be of different data types like string or Date, but must be in consistent format (e.g., "yymmdd", ""yyyy-mm-dd").}
}
\value{
A single character string:
\describe{
\item{\code{"YYYY-MM-DD"}}{when all dates in \code{df$date} are the same}
\item{\code{"YYYY-MM-DD_YYYY-MM-DD"}}{otherwise, concatenation of start and end dates}
}
Returns \code{NA_character_} (with a warning) if \code{df} has zero rows.
}
\description{
Computes the overall date range from the \code{date} column of the input data frame,
returning it as a single string. If all dates are identical, returns that single date.
}
\details{
\itemize{
\item Throws an error if \code{df} is not a data frame or lacks a \code{date} column.\cr
}
}
