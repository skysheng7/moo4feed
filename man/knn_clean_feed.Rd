% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/knn_outliers.R
\name{knn_clean_feed}
\alias{knn_clean_feed}
\title{Process multiple days of feeding data and remove outliers using KNN}
\usage{
knn_clean_feed(
  feed_data,
  k = 50,
  threshold_percentile = 99.936,
  custom_scaling = list(rate = 10000, intake = 7, duration = 0.03),
  intake_col = intake_col2(),
  duration_col = duration_col2(),
  remove_outliers = FALSE,
  date_col = "date"
)
}
\arguments{
\item{feed_data}{A list of daily feeding data frames or a single data frame.}

\item{k}{Integer. Number of nearest neighbors to consider (default: 50).
Will be automatically adjusted if it exceeds the number of rows in the data.}

\item{threshold_percentile}{Numeric. Percentile threshold for outlier detection.
Points with average distances above this percentile are considered outliers.
Must be between 0 and 100. Default is 99.}

\item{custom_scaling}{A named list with scaling factors for input variables.
Default is NULL, which means no scaling is applied (all factors = 1).}

\item{intake_col}{Character. Name of the column containing intake data (default: from global_var.R).}

\item{duration_col}{Character. Name of the column containing duration data (default: from global_var.R).}

\item{remove_outliers}{Logical. Whether to remove outliers from the data frame.}

\item{date_col}{Character. Name of the date column if feed_data is a list that needs to be unmerged (default: "date").}
}
\value{
If input is a list: a list of data frames with outliers detected.
If input is a data frame: a data frame with outliers detected.
If remove_outliers=TRUE, returns data with outliers removed.
}
\description{
Process multiple days of feeding data and remove outliers using KNN
}
