% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/daylight_saving.R
\name{dst_switch_day}
\alias{dst_switch_day}
\title{Detect Daylight Saving Time (DST) Change Dates}
\usage{
dst_switch_day(years = c(2020, 2021), tz = Sys.timezone())
}
\arguments{
\item{years}{A numeric vector specifying the range of years to evaluate.
Use the format \code{c(start_year, end_year)}, e.g., \code{years = c(2020, 2021)}.
Must contain at least one numeric value.}

\item{tz}{A valid time zone name (default is the time zone of your current physical location),
used to determine DST rules. Use \code{OlsonNames()} to see all valid options.}
}
\value{
A data frame with columns: year, spring (DST start for global north),
fall (DST end for global south), spring_next_day (the second day after daylight
saving changes in spring), fall_next_day (the second day after daylight saving
changes in fall). If no DST transitions are found for the specified years and
time zone, an empty data frame is returned with the expected column names.
}
\description{
Returns a data frame with the start ("spring") and end ("fall") dates of Daylight
Saving Time for each year in the input range.
}
\details{
If \code{years} is not numeric, has zero length, <= 1907 (start year of daylight
saving changes in history) or if \code{tz} is not a recognized time zone name,
the function will throw an error. For regions without DST (e.g., \code{Etc/UTC}),
the function returns an empty data frame and issues a warning.
}
\examples{
dst_switch_day(years = c(2020, 2021), tz = "America/Vancouver")
}
