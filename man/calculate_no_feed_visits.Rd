% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/non_nutritive_visits.R
\name{calculate_no_feed_visits}
\alias{calculate_no_feed_visits}
\title{Calculate visits with no feed available per animal per day}
\usage{
calculate_no_feed_visits(
  data,
  cfg = qc_config(),
  id_col = id_col2(),
  intake_col = intake_col2(),
  start_weight_col = start_weight_col2()
)
}
\arguments{
\item{data}{A named list of daily data frames (one per day), each containing visit-level data.}

\item{cfg}{A configuration list created by \code{\link[=qc_config]{qc_config()}}.}

\item{id_col}{Animal ID column name (default current global value from \code{\link[=id_col2]{id_col2()}})}

\item{intake_col}{Intake column name (default current global value from \code{\link[=intake_col2]{intake_col2()}})}

\item{start_weight_col}{Start weight column name (default current global value from \code{\link[=start_weight_col2]{start_weight_col2()}})}
}
\value{
A named list of data frames, one per day, each containing columns
for animal ID and the count of visits with no feed available.
}
\description{
Calculates the number of visits for each animal on each day
when there was no feed available (start weight less than or equal to
calibration error and intake less than or equal to calibration error).
}
\examples{
toy_data <- list(
  "2023-01-01" = data.frame(
    cow = c("1", "2", "3", "1"),
    intake = c(0.2, 0.3, 0.4, 0.0),
    start_weight = c(0.3, 0.2, 0.1, 0.4)
  )
)
cfg <- qc_config(calibration_error = 0.5)
result <- calculate_no_feed_visits(toy_data, cfg = cfg)
result[[1]]

}
