% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unique_bin_visits.R
\name{unique_bin_visits}
\alias{unique_bin_visits}
\title{Analyze Bin Visit Patterns For Each Cow}
\usage{
unique_bin_visits(
  feed = NULL,
  water = NULL,
  id_col = id_col2(),
  bin_col = bin_col2(),
  bin_offset = bin_offset2(),
  bins_feed = bins_feed2(),
  bins_wat = bins_wat2(),
  return_list = FALSE
)
}
\arguments{
\item{feed}{A list of daily \strong{feed} data frames named by date, or \code{NULL}
if you don't have feeder data}

\item{water}{A list of daily \strong{water} data frames named by date, or \code{NULL}
if you don't have water data}

\item{id_col}{Animal ID column name (default current global value)}

\item{bin_col}{Bin ID column name (default current global value)}

\item{bin_offset}{Numeric bin offset (default current global value)}

\item{bins_feed}{Integer vector of feed bins (default current global value)}

\item{bins_wat}{Integer vector of water bins (default current global value)}

\item{return_list}{Logical. Whether to return a list of dataframes or a single dataframe.}
}
\value{
A dataframe with columns for date, cow ID, number of unique feed bins visited,
number of unique water bins visited, and total unique bins visited.
}
\description{
This function analyzes how cows interact with feed and water bins
across multiple days. For each day and each cow, it calculates:
\itemize{
\item The number of unique feed bins visited
\item The number of unique water bins visited
\item The total number of unique bins visited
}
}
\examples{
# With separate feed and water data lists
bin_visit_stats <- unique_bin_visits(feed = all_fed, 
                                     water = all_wat,
                                     id_col = "cow",
                                     bin_col = "bin",
                                     bin_offset = 100,
                                     bins_feed = 1:30,
                                     bins_wat = 1:30,
                                     return_list = FALSE)

# View the results
head(bin_visit_stats)

}
