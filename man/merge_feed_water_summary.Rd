% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge_feed_water_summary.R
\name{merge_feed_water_summary}
\alias{merge_feed_water_summary}
\title{Summarize and check feed & water intake}
\usage{
merge_feed_water_summary(feed = NULL, water = NULL, warn, cfg = qc_config())
}
\arguments{
\item{feed}{A data frame of feed visits.}

\item{water}{A data frame of drinking visits.}

\item{warn}{A warning skeleton (e.g., from \code{qc_warning_skeleton()}).}

\item{cfg}{A config list. See \code{\link[=qc_config]{qc_config()}}.}
}
\value{
A list of:
\itemize{
\item \code{summary}: merged data frame of daily intake, duration, and visit counts
\item \code{warn}: updated warnings
}
}
\description{
This function summarizes daily feed and water intake, visit duration, and counts,
and updates intake warnings.
}
\examples{
feed <- tibble::tibble(
  date = as.Date(c("2024-01-01", "2024-01-01")),
  cow = c("A", "B"),
  intake = c(20, 80),
  duration = c(200, 300)
)

water <- tibble::tibble(
  date = as.Date(c("2024-01-01", "2024-01-01")),
  cow = c("A", "B"),
  intake = c(50, 200),
  duration = c(100, 150)
)

warn <- tibble::tibble(
  date = as.Date("2024-01-01"),
  low_daily_feed_intake_cows = NA_character_,
  high_daily_feed_intake_cows = NA_character_,
  low_daily_water_intake_cows = NA_character_,
  high_daily_water_intake_cows = NA_character_
)

cfg <- qc_config()
merge_feed_water_summary(feed, water, warn, cfg)

}
